[package]
name = "toke"
version = "0.1.0"
edition = "2024"
description = "A DSL that compiles to ERC-20 EVM bytecode"

[dependencies]
tiny-keccak = { version = "2", features = ["keccak"] }
hex = "0.4"

[dev-dependencies]
revm = "36"
